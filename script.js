document.addEventListener('DOMContentLoaded', function() {
    // Get references to the menu links
    const homeLink = document.querySelector('a[href="#Home"]');
    const aboutusLink = document.querySelector('a[href="#About"]');
    const reviewLink = document.querySelector('a[href="#Review"]');
    const menuLink = document.querySelector('a[href="#Menu"]');
    const orderLink = document.querySelector('a[href="#Order"]');
    const galaryLink = document.querySelector('a[href="#Gallary"]');

    // Define a function to open a file in a new tab
    function openFileInNewTab(file) {
        window.open(file, '_blank');
    }

    // Add click event listeners to the menu links
    homeLink.addEventListener('click', function() {
        openFileInNewTab('home.html');
    });

    aboutusLink.addEventListener('click', function() {
        openFileInNewTab('about-us.html');
    });

    reviewLink.addEventListener('click', function() {
        openFileInNewTab('review.html');
    });

    menuLink.addEventListener('click', function() {
        openFileInNewTab('menu.html');
    });

    orderLink.addEventListener('click', function() {
        openFileInNewTab('order.html');
    });

    // Add click event listener for the Gallary link
    galaryLink.addEventListener('click', function() {
        openFileInNewTab('galary.html');
    });

    var facebookIcon = document.getElementById('facebookIcon');

    // Add a click event listener to the Facebook icon
    facebookIcon.addEventListener('click', function() {
        // Navigate to the Facebook page when the icon is clicked
        window.location.href = 'https://www.facebook.com';
    });
    var twitterIcon = document.getElementById('twitterIcon');

    // Add a click event listener to the Facebook icon
    twitterIcon.addEventListener('click', function() {
        // Navigate to the Facebook page when the icon is clicked
        window.location.href = 'https://www.twitter.com';
    });
    var instagramIcon = document.getElementById('instagramIcon');

    // Add a click event listener to the Facebook icon
    instagramIcon.addEventListener('click', function() {
        // Navigate to the Facebook page when the icon is clicked
        window.location.href = 'https://www.instagram.com';
    });

    var facebookIcon = document.getElementById('facebookIcon4');

    // Add a click event listener to the Facebook icon
    facebookIcon.addEventListener('click', function() {
        // Navigate to the Facebook page when the icon is clicked
        window.location.href = 'https://www.facebook.com';
    });
    var twitterIcon = document.getElementById('twitterIcon4');

    // Add a click event listener to the Facebook icon
    twitterIcon.addEventListener('click', function() {
        // Navigate to the Facebook page when the icon is clicked
        window.location.href = 'https://www.twitter.com';
    });
    var instagramIcon = document.getElementById('instagramIcon4');

    // Add a click event listener to the Facebook icon
    instagramIcon.addEventListener('click', function() {
        // Navigate to the Facebook page when the icon is clicked
        window.location.href = 'https://www.instagram.com';
    });

    ///////////////////
    var facebookIcon = document.getElementById('facebookIcon3');

    // Add a click event listener to the Facebook icon
    facebookIcon.addEventListener('click', function() {
        // Navigate to the Facebook page when the icon is clicked
        window.location.href = 'https://www.facebook.com';
    });
    var twitterIcon = document.getElementById('twitterIcon3');

    // Add a click event listener to the Facebook icon
    twitterIcon.addEventListener('click', function() {
        // Navigate to the Facebook page when the icon is clicked
        window.location.href = 'https://www.twitter.com';
    });
    var instagramIcon = document.getElementById('instagramIcon3');

    // Add a click event listener to the Facebook icon
    instagramIcon.addEventListener('click', function() {
        // Navigate to the Facebook page when the icon is clicked
        window.location.href = 'https://www.instagram.com';
    });
    /////////////////////
    var facebookIcon = document.getElementById('facebookIcon2');

    // Add a click event listener to the Facebook icon
    facebookIcon.addEventListener('click', function() {
        // Navigate to the Facebook page when the icon is clicked
        window.location.href = 'https://www.facebook.com';
    });
    var twitterIcon = document.getElementById('twitterIcon2');

    // Add a click event listener to the Facebook icon
    twitterIcon.addEventListener('click', function() {
        // Navigate to the Facebook page when the icon is clicked
        window.location.href = 'https://www.twitter.com';
    });
    var instagramIcon = document.getElementById('instagramIcon2');

    // Add a click event listener to the Facebook icon
    instagramIcon.addEventListener('click', function() {
        // Navigate to the Facebook page when the icon is clicked
        window.location.href = 'https://www.instagram.com';
    });
    ///////////
    var facebookIcon = document.getElementById('facebookIcon1');

    // Add a click event listener to the Facebook icon
    facebookIcon.addEventListener('click', function() {
        // Navigate to the Facebook page when the icon is clicked
        window.location.href = 'https://www.facebook.com';
    });
    var twitterIcon = document.getElementById('twitterIcon1');

    // Add a click event listener to the Facebook icon
    twitterIcon.addEventListener('click', function() {
        // Navigate to the Facebook page when the icon is clicked
        window.location.href = 'https://www.twitter.com';
    });
    var instagramIcon = document.getElementById('instagramIcon1');

    // Add a click event listener to the Facebook icon
    instagramIcon.addEventListener('click', function() {
        // Navigate to the Facebook page when the icon is clicked
        window.location.href = 'https://www.instagram.com';
    });

});